/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "oaidallleapiproduscus.blob.core.windows.net",
        ]
    }
}

module.exports = nextConfig
